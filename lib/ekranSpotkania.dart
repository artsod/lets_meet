import 'package:flutter/material.dart';

class EkranSpotkania extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Tu będzie zarządzanie spotkaniem'),
      ),
    );
  }
}