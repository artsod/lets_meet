import 'package:flutter/material.dart';

class EkranUstawienia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tutaj będzie zarządzanie ustawieniami'),
      ),
    );
  }
}